# Web Framework
Flask>=3.1.1
gunicorn>=23.0.0

# HTTP Requests
requests>=2.32.4

# Optional but Recommended
# For geocoding, caching, or future extensions
geopy>=2.4.1        # if you later use Python-based geocoding
flask-cors>=5.0.0   # if you want to allow cross-origin (useful if JS calls API directly)
python-dotenv>=1.0.1 # for environment variable handling

# Utilities (not required but safe to include)
Jinja2>=3.1.6
Werkzeug>=3.1.3
click>=8.2.1
